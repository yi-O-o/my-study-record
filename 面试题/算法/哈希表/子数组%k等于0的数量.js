/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
 var subarraysDivByK = function(nums, k) {
    
};

let nums = [4,5,0,-2,-3,1] 
let k = 5
let map = new Map()
let befSum =0
let count = 0
for(let i = 0; i<nums.length; i++){
    befSum += nums[i]
    if(map.has())
}